// On travaille entre if et endif, si MODULE_H n'est pas importé, on l'importe ifndef : ifnotdef
#ifndef MODULE_H
// Define : Remplace MODULE_H par int addone(int x);
#define MODULE_H 

int addone(int x);

#endif

